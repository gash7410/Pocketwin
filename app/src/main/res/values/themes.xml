<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real theming (see ui/theme/Theme.kt); this is just the
         pre-Compose window background so cold start doesn't flash white/black. -->
    <style name="Theme.PocketWin" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
    </style>
</resources>
